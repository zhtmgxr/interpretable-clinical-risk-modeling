# Data access

Patient level data are not distributed with this repository.

To run the analysis, provide an anonymized CSV that follows `data_dictionary.csv`. Store local data under `data/private/`, which is excluded from version control.

The analysis expects one row per patient and does not require names, medical record numbers, dates of birth, admission dates, or free text notes.
